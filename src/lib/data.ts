import type { Category, Video } from '@/types';

// This file is deprecated. Video data is now fetched directly from the Mux API
// via server actions in /src/app/actions/mux.ts.
export const videos: Video[] = [];

export const categories: Category[] = [];
